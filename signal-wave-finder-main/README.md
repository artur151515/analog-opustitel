# Opustoshitel - Торговые сигналы

Фронтенд-приложение для отображения торговых сигналов бинарных опционов с интеграцией TradingView.

## 🚀 Быстрый старт

### Установка зависимостей

```bash
npm install
# или
pnpm install
```

### Запуск в режиме разработки

```bash
npm run dev
# или
pnpm dev
```

Приложение будет доступно по адресу: `http://localhost:8080`

## 📋 Структура проекта

```
src/
├── components/        # React компоненты
│   ├── ui/           # UI компоненты (shadcn)
│   ├── SignalCard.tsx
│   ├── StatsPanel.tsx
│   ├── TVChart.tsx
│   ├── SymbolSelector.tsx
│   ├── TfSelector.tsx
│   └── Disclaimer.tsx
├── pages/            # Страницы приложения
│   ├── Landing.tsx   # Главная страница
│   ├── Signals.tsx   # Страница сигналов
│   └── NotFound.tsx
├── hooks/            # Custom React hooks
│   └── useSignalData.ts
├── types/            # TypeScript типы
│   └── signal.ts
└── App.tsx           # Главный компонент
```

## 🔌 API Endpoints

### Получение сигнала

```
GET /api/signal?symbol=CADJPY&tf=5m
```

Ответ:
```json
{
  "symbol": "CADJPY",
  "tf": "5m",
  "direction": "UP",
  "confidence": 63.5,
  "enter_at": "2025-10-02T20:01:00Z",
  "expire_at": "2025-10-02T20:06:00Z",
  "generated_at": "2025-10-02T20:00:00Z"
}
```

### Получение статистики

```
GET /api/stats?symbol=CADJPY&tf=5m
```

Ответ:
```json
{
  "winrate_last_N": 58.2,
  "N": 200,
  "break_even_at_085": 54.05,
  "signals_count": 1247
}
```

## ⚙️ Настройка

### Символы и таймфреймы

Доступные символы настраиваются в `src/components/SymbolSelector.tsx`:
```typescript
const symbols = [
  { value: 'CADJPY', label: 'CAD/JPY' },
  { value: 'GBPJPY', label: 'GBP/JPY' },
  { value: 'EURUSD', label: 'EUR/USD' },
];
```

Доступные таймфреймы в `src/components/TfSelector.tsx`:
```typescript
const timeframes: TimeFrame[] = ['3m', '5m', '7m', '10m'];
```

### URL API

По умолчанию используется относительный путь (`/api`). Для изменения базового URL API отредактируйте `src/hooks/useSignalData.ts`:

```typescript
const baseUrl = apiUrl || 'https://your-api-url.com';
```

## 🎨 Дизайн-система

Приложение использует тёмную тему с красными акцентами. Все цвета определены в `src/index.css` и `tailwind.config.ts`:

- **Primary**: Красный (#ef4444) - основной акцентный цвет
- **Success**: Зелёный - для UP сигналов
- **Destructive**: Красный - для DOWN сигналов
- **Background**: Тёмно-серый градиент

## 📱 Функциональность

### Автообновление
- Данные обновляются автоматически каждые 10 секунд
- Используется AbortController для отмены предыдущих запросов

### LocalStorage
- Выбранный символ и таймфрейм сохраняются локально
- Восстанавливаются при следующем посещении

### Countdown таймер
- Показывает время до экспирации сигнала
- Прогресс-бар визуализирует оставшееся время
- Автоматический refresh после экспирации

### TradingView интеграция
- Встроенный виджет TradingView
- Автоматическое переключение символа и таймфрейма

## 🛡️ Обработка ошибок

При недоступности API:
- Показываются mock-данные для демонстрации
- Выводится toast с уведомлением об ошибке
- Приложение остаётся функциональным

## 📦 Production Build

```bash
npm run build
# или
pnpm build
```

Результат будет в директории `dist/`.

## 🔒 Безопасность

- Нет ключей API на клиенте
- Только публичные GET запросы
- Все данные валидируются TypeScript типами

## 📄 Лицензия

© 2025 Opustoshitel. Все права защищены.

---

**Важно**: Приложение не является финансовой рекомендацией. 18+
